const primaryColor = "#1e1e1e";
const secondaryColor = "#42a07e";

const contrastColor = "#ffffff";

const cssThemes = {
  "--primary-color": primaryColor,
  "--secondary-color": secondaryColor,
  "--contrast-color": contrastColor,
};

const themes = {
  primaryColor,
  secondaryColor,
  contrastColor,
};

export { cssThemes, themes };
